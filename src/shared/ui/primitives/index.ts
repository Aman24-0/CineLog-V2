// src/shared/ui/primitives/index.ts
// Barrel export for shared primitive UI components.
// Only components actually consumed by feature pages are exported.
export { default as Button } from "./Button";
export { default as Skeleton } from "./Skeleton";
